import React from 'react'
import MatchInfo from './MatchInfo'

import './MatchHistory.css'

export default function MatchHistory({matchList}) {
  console.log("match List", matchList)
  // const isMatchListLoading = matchList.length === 0
  // console.log("isMatchLoading", isMatchLoading)
  return (
  <div>test</div>
  )
    // ( isMatchListLoading 
    //     ? (<div>loading</div>)
    //     : (<div className="match-history">
    //   {
    //     matchList.map((match, idx) => (
    //       <MatchInfo match={match} key={`match-${idx}`}/>
    //     )
    //   )}
    // </div>)
    // )
   
  
}
